import 'package:flutter/material.dart';

class MusicPlayerTheme {
  final primaryListTileColor = Color.fromARGB(255, 13, 13, 172);
  final primaryAppBarColor = Colors.green;
  final primaryBottomSheetColor = Colors.green;
  final primaryDefaultArtworkBackgroundColor = Color.fromARGB(255, 0, 0, 0);
  final primaryColor = Color.fromARGB(255, 0, 0, 0);
  final secondaryColor = Color.fromARGB(255, 29, 15, 15);
  final tertiaryColor = Color.fromARGB(255, 230, 226, 17);
  final buttonColor = Colors.green;

  final progressBaseColor = Color.fromARGB(255, 240, 240, 240);

  final progressBarColor = Color.fromARGB(255, 0, 0, 0);

  final musicVisualiser = Colors.green;
  final selectedTile = Color.fromARGB(255, 0, 0, 0);
  final iconColor = Color.fromARGB(255, 0, 0, 0);

  final linearGradientBody = const LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [
      Color.fromARGB(255, 255, 255, 255),
      Color.fromARGB(255, 255, 255, 255),
    ],
  );
}
